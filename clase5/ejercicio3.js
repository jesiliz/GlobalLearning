//Ejercicio 3: Crear una función que reciba dos parámetros. Un número y un string (texto). 
//Luego debe repetir ese string tantas veces como lo diga el número recibido.

function numeroPalabra(num,nomb)
{
    for(i=0;i<num;i++)
    {
        console.log(nomb);
    }
}
numeroPalabra(3,'Hola!! estoy aprendiendo Node js')